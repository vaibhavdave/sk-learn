numpy
pandas
matplotlib
seaborn
scikit-learn
imbalanced-learn
jupyter
notebook
torch
torchinfo
# tensorflow-cpu avoids noisy "no CUDA drivers found" logs on machines without a GPU.
# If you have an NVIDIA GPU + CUDA installed, swap this for the regular `tensorflow` package instead.
tensorflow-cpu
